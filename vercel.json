{
  "framework": null,
  "installCommand": "echo Skipping npm install — static Babel app",
  "buildCommand": "node scripts/write-site-config.mjs && node scripts/write-learned-corrections-config.mjs",
  "outputDirectory": "."
}
